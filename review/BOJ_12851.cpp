#include <iostream>
#include <queue>

typedef unsigned int u32;

typedef struct StructEntry
{
    int x;
    u32 w;
} Entry;

int main()
{
    std::ios_base::sync_with_stdio(false);
    std::queue<Entry> que;

    int N, K;

    std::cin >> N >> K;
    
}